#import "../fragments/pageInfo.gql"

query studentsFilter(
  $first: Int = 25,
  $where: StudentFilterInput
) {
  students(
    first: $first,
    where: $where
  ) {
    pageInfo {
      ...PageInfoParts
    }
    totalCount
    nodes {
      accountNumber
    }
  }
}
