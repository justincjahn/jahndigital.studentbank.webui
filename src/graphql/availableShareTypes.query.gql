query availableShareTypes($first: PaginationAmount) {
  availableShareTypes(first: $first) {
    pageInfo {
      startCursor
      endCursor
      hasNextPage
      hasPreviousPage
    }
    totalCount
    nodes {
      id
      name
      dividendRate
      shareTypeInstances {
        instanceId
      }
    }
  }
}