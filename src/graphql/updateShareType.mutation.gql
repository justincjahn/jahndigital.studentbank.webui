mutation updateShareType($id: Long!, $name: String, $dividendRate: Rate) {
  updateShareType(input: { id: $id, name: $name, dividendRate: $dividendRate}) {
    id
    name
    dividendRate
    shareTypeInstances {
      instanceId
    }
  }
}
