mutation linkShareType($shareTypeId: Long!, $instanceId: Long!) {
  linkShareType(input: { shareTypeId: $shareTypeId, instanceId: $instanceId }) {
    id
    name
    dividendRate
    shareTypeInstances {
      instanceId
    }
  }
}
